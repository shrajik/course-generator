# Base URL of the FastAPI backend, as seen from the user's BROWSER.
# With docker compose this stays localhost (not the internal service name),
# because every request in this app is made client-side.
NEXT_PUBLIC_API_URL=http://localhost:8000
